import { RenderHighlighterDirective } from './render-highlighter.directive';

describe('RenderHighlighterDirective', () => {
  it('should create an instance', () => {
    const directive = new RenderHighlighterDirective();
    expect(directive).toBeTruthy();
  });
});
